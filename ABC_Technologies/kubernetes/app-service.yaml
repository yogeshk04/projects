apiVersion: v1
kind: Service
metadata:
  name: abctech-service  
  labels:
    app: abctech
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: abctech
  sessionAffinity: None